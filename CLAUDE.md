# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際の前提知識・方針をまとめたもの。実装前に必ず目を通すこと。

## プロジェクトの目的

Nintendo Switch Sports「サッカー」のプレイ映像をキャプチャーボード経由でリアルタイム解析し、勝敗・ランク変動を自動検知してSQLiteに記録する。将来的には配信画面にグラフを表示する。

現在のスコープは「勝敗」と「ランク(∞帯の数値・ゲージ)」の記録のみ。ゴール・アシストの検知は将来段階であり、今は実装しない。

## 確定している設計方針

### 実行環境
- Windows のみを想定(Mac/Linux対応は考えなくてよい)
- キャプチャーボード: I-O DATA GV-USB3HDS/E、キャプチャ解像度は1920x1080を想定

### 映像取得の構成
- Python側は物理キャプチャーデバイスを直接開かない。OBS Studio の **Virtual Camera** 出力を映像ソースとして利用する
  - 理由: UVCデバイスは基本的に単一アプリしか掴めないため、OBSが配信・録画のために専有している間もPythonが同じ映像を取得できるようにするため
- リアルタイム処理を行う(録画ファイルを後から処理する方式ではない)

### 検知方式の方針
- 勝敗判定: 画面上部に出る斜めの結果バナーの**色・形状**で判定する(OCRではない)。実測では勝ち=緑〜ティール系、負け=黒系の配色で、チームカラー(青/赤/ピンク)には依存しない
- ランク数値: 画面左下の固定領域をOCRで読み取る(EasyOCR等を想定)
- 状態監視(アニメーション中かどうかの判定)は固定領域のピクセル差分・平均色で行う。軽量な処理なので高頻度に回してよい

### サンプリング戦略(重要)
- 「軽量な監視」(色判定・ピクセル差分)はキャプチャの生フレームレートのまま(30fps想定)常時実行してよい。負荷はほぼ無視できる
- 「重い処理」(OCR)は常時実行しない。軽量監視により「数値が変化しなくなった=確定」を検知した瞬間に**1回だけ**発火させる
- この2層構成(常時軽量監視 + イベント駆動の重い処理)を崩さないこと

### 試合後の状態遷移
1. 結果バナー表示 → 勝敗+開始時ランクを記録
2. ランク変動アニメーション → 数値が安定するまで監視(OCRは確定時のみ)
3. ランク確定 → 最終値、リーグ昇格/降格の有無を記録
   - リーグ昇格/降格の演出画面は、ランクの帯(数値)が実際に変化した場合のみ表示される特別イベント。通常はゲージの増減のみ
4. 暗転 → ここでクールダウン(この試合の記録完了とみなす)
5. マッチング画面 → 次の試合の検知を再開

### データ保存
- SQLite。詳細スキーマは `README.md` のドラフトを参照(未確定、実装時に精査すること)

### 将来の拡張(今は実装しないが構成に影響する)
- 配信画面へのグラフ表示: ローカルWebサーバー(FastAPI/Flask想定)がSQLiteの内容をHTML+JSで可視化し、OBSの「ブラウザソース」として表示する
- そのため、検知・記録処理とWebサーバー処理は将来同一プロセス内で(スレッド分けして)同時に動かせるような構成を意識しておくこと

## 未確定・要相談(実装前に確認すること)

<!-- TODO: 依存関係管理ツール(pip+requirements.txt / poetry / uv)を決める -->
<!-- TODO: src/ 以下のモジュール構成(capture / detection / storage / web の分割方法)を決める -->
<!-- TODO: テスト方針(fixtures/screenshots を使ったユニットテストの組み方)を決める -->
<!-- TODO: ログ出力の方針(標準出力 / ファイル / ログレベル)を決める -->
<!-- TODO: 常駐アプリとしての起動方法(手動スクリプト実行 / タスクスケジューラ等)を決める -->

## 参照ドキュメント

- [`docs/screen_states.md`](docs/screen_states.md): 画面状態の一覧(Obsidianのメモから移行)。チェック済み項目 = 参照画像が `fixtures/screenshots/` に用意済み
- [`docs/git_workflow.md`](docs/git_workflow.md): コミット・ブランチ運用ルール
- [`README.md`](README.md): プロジェクト概要とアーキテクチャ図

## Claude Codeへの依頼時の注意

- 本ファイルに書かれた「確定している設計方針」は覆さないこと。変更が必要な場合は提案として提示し、実装前に確認を取る
- 「未確定・要相談」の項目は、着手前にユーザーに確認してから進める
- `fixtures/screenshots/` の画像は実機キャプチャの参照データなので、テストや検知ロジックの調整に積極的に使ってよい
- ただし **`fixtures/screenshots/` は `.gitignore` 対象(ローカルのみ、リモートには含めない)**。他プレイヤーの映り込みやプレイヤー名が理由なので、この画像群を誤ってコミットしたりリモートに送信する処理を書かないこと。ファイル名の一覧・命名規則は `docs/screen_states.md` を参照すること
